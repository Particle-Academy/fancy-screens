/**
 * @particle-academy/fancy-screens
 *
 * Containerized application surface for fancy components.
 *
 * v0.1.0 is the bootstrap publish — exports are stubs while the runtime
 * lands phase by phase. Each subsequent minor release adds a layer:
 *
 *   0.2.x  port store + <Screen.Port>
 *   0.3.x  visibility + hibernation
 *   0.4.x  loading layer + <Screen.Loading>
 *   0.5.x  layouts (Stack / Grid / Spotlight / Group)
 *   0.6.x  schema-driven rendering
 *   0.7.x  url sync + sessionStorage persistence
 *   1.0.0  full v1
 *
 * See https://github.com/Particle-Academy/fancy-screens for the plan.
 */

/** Bootstrap stub — replaced by the real `<Screen>` component in 0.2.x. */
export const __SCAFFOLD__ = "@particle-academy/fancy-screens v0.1.0 — scaffold only";
